@{
    ViewBag.Title = "update";
}

<div class="alert alert-primary text-center" role="alert" style=" color: #004085; background-color: #cce5ff; border-color: #b8daff; ">
    <h1> @ViewBag.file</h1>
</div>
@ViewBag.Text2

<div class="col-lg-12 row">
    <div class="col-lg-4"></div>
    <div class="col-lg-4">
        <form id="form1" action="@Url.Action("update2")" method="post">
            <div class="form-group">
                <label for="txtResourceValue">
                    you can write a new value
                </label>
                <input type="hidden" name="Id" value="@ViewBag.Id" />
                <input type="hidden" name="file" value="@ViewBag.file" />
                <input id="txtResourceValue" name="txtResourceValue" class="form-control" value="@ViewBag.Text" />
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
    <div class="col-lg-4"></div>
</div>